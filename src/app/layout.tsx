import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "MyTvList",
  description: "“My TV List” is a web application that provides a platform for TV show enthusiasts to discover, follow, and rate shows. It also fosters a community where users can join various groups and engage in discussions about their favorite shows.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
